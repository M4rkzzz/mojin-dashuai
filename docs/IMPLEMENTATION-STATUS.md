# 魔金大帅实施状态

2026-09-05。此记录区分代码完成、隔离测试和正式交付，不能替代三服最终验收。

## 已完成

- 产品名「魔金大帅」，黑色底、扁平控件、单色矢量图标，无 emoji 和装饰说明小字。
- 登录、注册、恢复、大厅、三服详情、线路、设置及下载状态已实现。
- 首次在本机登录成功后先设置游戏文件保存位置，可输入路径或打开文件夹选择器；确认前不能安装或启动。设置后自动记忆，恢复未完成的登录仍返回目录页；旧版已保存的内容目录沿用。取消选择不跳过，非空目录与写入失败不会完成设置。
- 原生宿主移除系统标题栏，开启 WebView2 `IsNonClientRegionSupportEnabled`，使用 `app-region` 和 WPF `WindowChrome`。topbar 左侧不重复品牌名，右侧按钮同步最大化状态。
- 页面滚动限制在内容区；登录表单在 1280×820、1024×656、820×650、900×600 下没有外层或横向溢出。
- 皮肤半身头像替代文字头像，支持标准、纤细、64×32 旧版纹理和外层纹理，本地 Canvas 绘制。微软账号读取官方皮肤，自建账号可上传 PNG；无皮肤时显示默认占位人物。
- 本地 UI 不接收登录令牌；皮肤下载不附带账号凭据。原生层验证纹理地址和 PNG，服务器限制尺寸、解压量、CRC，并清除图片附带元数据。
- C#/.NET 10 + React/TypeScript/WebView2 自包含 Windows 包可构建。UI 资源本地加载，Java 与三服内容不包含在初始包中。
- Identity + PostgreSQL 普通/超级邀请码、固定游戏名、保护名、恢复码、会话轮换、撤销、管理员 SSH 命令已实现。
- 124 的独立 `mc-client-hub` Compose 已部署 API 0.1.2、PostgreSQL、downloads。API 为 `127.0.0.1:18081`，下载同时绑定 `127.0.0.1:18080` 和 `192.168.5.124:18080`。
- 下载公网入口 `http://103.40.14.100:21708` 已接通，13:17 实测健康检查、64 KiB 文件 SHA256、HTTP Range 续传通过。补充回环监听时仅重建 downloads，账号及游戏容器均未重启。
- 23 个原名已保护，另 1 组大小写冲突已整体保留，普通码和超级码均不能认领；未选择角色、合并身份或改动存档。
- 每日数据库备份保留七份，皮肤目录纳入每日备份；API 升级前额外保留数据库快照。此次升级核对其他容器的启动时间未变化。
- 私有仓库 https://github.com/M4rkzzz/mojin-dashuai 已推送初始实现，首次 Windows/API Actions 均通过。后续检查增加无界面 UI 验证。

- 已安装实例启动前自动检查新版，先完成差异下载再启动。暂停与续传保持自动启动意图；临时目录服务故障可使用已安装版本，签名与内容异常不被吞掉。显式授权的回退版本可保持到下次正式版本变化。
- 修改群服密码后原生撤销会话并通知前端返回登录页，清除旧头像和恢复码界面状态。

## 已有测试结果

- 新增设备码页面无界面测试：开始、复制、主动打开网页、授权后目录页、登录码出现前后取消、过期及应用错误；1280/820/640 宽度与 125% 缩放通过。页面不使用 localStorage 或 sessionStorage 保存任何登录信息。

- 76 项 .NET 测试通过：签名、路径、运行时约束、下载、事务恢复、皮肤、首次目录设置，以及设备码公开字段、微软验证地址、Xbox/Minecraft 请求格式、各阶段凭据隔离、取消、所有权、错误处理和公开签名元数据约束。
- 124 隔离数据库和 API 容器中 39 项验收通过：邀请码、保护名及大小写冲突、恢复、会话、禁用、限流、秘密不入日志/数据库明文；皮肤鉴权上传、公开无凭据读取、账号隔离及无效更新保留旧皮肤；未发布目录、文件清单、公开元数据原样读取与路径限制。
- 20 项 Windows 原生无窗口检查通过：真实 CmlLib 依赖构造微软授权地址、随机 state 与固定回调检查、取消授权、DPAPI 缓存暂存/提交/恢复/退出。全部使用隔离测试凭据，不代表真人微软登录验收。
- 无界面浏览器通过 12 项登录/注册/恢复布局检查，以及大厅、设置、详情、缩放、减少动画检查。
- 标准、纤细及旧版皮肤使用合成测试纹理核对脸部、衣服、手臂像素和半身显示；未使用真实玩家账号冒充验收。
- 无界面测试覆盖群服登录、微软登录及注册后的目录页、恢复码先保存、选择器取消、写入错误、键盘确认、重新打开后记忆和旧用户直接进入大厅，窗口宽度 1280/820/640、125% 像素缩放下通过检查。账号与选择器使用测试替身，不代替真实微软登录或原生文件夹对话框验收。
- 新 Windows 自包含预览位于 `artifacts/魔金大帅-0.1.1-preview/`。按用户正在玩 CS 的要求，不自动启动窗口；新标题栏的实际拖动、最大化、跨显示器缩放仍待桌面实测。

## 三服内容

- 标准分发工具已经落地，见 `docs/STANDARD-DISTRIBUTIONS.md`。一、二服为 Modrinth mrpack；三服为官方 Cleanroom MMC 补丁实例 + packwiz，不能声称 PCL 可直接导入三服。源码按固定 PCL 提交核对，没有复制 PCL 代码或私有凭据。
- 二服 317 个固定文件、三服 377 个模组均已完成实际原站下载及哈希验证；一服 117 个文件（包含子目录依赖）中 99 个已完成原站验证，另 18 个已单独托管，没有待补来源文件。详细缺项保存在 `packs/standard-distribution-status.json`。
- 自建 objects HTTPS 路由接通。三个一服备用文件和 ConfigAnytime 测试备用文件按 SHA256 命名，保留许可文字；完整下载和 Range 206 通过。原生 Downloader 在第一个真实 URL 返回 404 后切换自建源并校验成功。未重启游戏服务。
- 9 项标准格式检查和 73 项 .NET 检查通过；真实固定版本 packwiz 在隔离目录的无界面安装测试通过，验证清单、自建下载、配置安装和已有玩家设置保留。这些结果不替代三服游戏验收。
- 本地标准包和 public/distributions 暂存目录位于 `artifacts/distributions/`，尚未公开发布整合包或正式 catalog。三个服均已生成标准候选包，一服也已补齐来源，不再是 draft。Java 按独立清单下发；原生正式清单还需补齐游戏资源、加载器库、配置策略和最终入服验收。

- 最初按本地文件 SHA1 与大小匹配 Modrinth 官方版本 API，确认一服 22 个、三服 131 个完全相同的作者文件，包括魔法金属 8.0.4-2。后续补充 CurseForge CDN、GitHub 及已核实许可的单文件备用源；当前数量以上面的标准分发记录为准。

- 一服来源为用户提供的 `D:/Desktop/魔金大帅`，MSE/魔法金属 8.0.4-2，114 个顶层活动 JAR 和 3 个子目录依赖；标准包排除个人数据和凭据。
- 二服 DeceasedCraft 5.10.17 中文 r3 的 317 个固定引用已完成自动下载和哈希核对，ModernFix 实际入服兼容仍待验收。
- 三服从桌面旧客户端整理独立 `.local/lab/mb`，使用官方 Cleanroom 0.5.17-alpha 与 Temurin 25.0.4.1+1，补齐安装器生成的库路径和原版 JAR 引用。已停用的机器注册项未重新引入。
- 三服隔离进程日志在 12:12:44 报告成功加载 422 个模组，12:17:56 正常退出，退出码 0。这只证明本地加载流程，不能当作入服、地图、任务书、机器或性能验收。
- Java 8、17 的官方分发包已下载并核对哈希；Java 25 使用已准备的固定发行版。三服严格禁止 Java 8/17 回退。

## 外部配置及剩余工作

- Cloudflare 凭据问题已解决。用户最新要求在 `D:/project/cfapi/credentials.json` 明文保存可复用 Global API Key；已保存并验证，不再依赖原 GitHub 项目。密钥文件位于 Git 仓库之外，未上传 GitHub 或 124。使用方式见 `docs/CLOUDFLARE-CREDENTIALS.md`。
- 已创建专用 Tunnel `2aef8300-5e56-4c26-9abd-0d3019c5fffd` 与 `launcher.boshan.uk` DNS。124 的 cloudflared 使用 HTTP/2，已建立两条连接；只有该容器启动/重建，其他容器启动时间未变化。配置规则只对该 API 主机关闭 Browser Integrity Check，保留其他域名规则。
- `https://launcher.boshan.uk/health` 与公告接口已返回 200。18 项公网及 Windows 原生检查通过：注册、固定游戏名、登录、刷新、退出、旧令牌撤销、原生 DPAPI 保存、恢复登录、游戏会话昵称；测试账号和邀请码已删除。目录接口仍按预期返回 503，因为正式游戏内容尚未验收发布。
- 正版登录默认接入 CmlLib.Core.Auth.Microsoft 3.3.0 文档提供的 Windows 登录方式，不需要用户创建 Azure 目录。独立 WPF/WebView2 授权窗口没有启动器桥接，使用独立 InPrivate 浏览器配置；随机 state 与固定回调均验证，原生缓存使用 Windows DPAPI，登录完整成功前不覆盖旧缓存，刷新不弹窗。额外检查 Minecraft 所有权并保存实际角色 UUID、名字和 XUID。显式配置有效 `microsoftClientId` 时仍支持自有应用的 MSAL 设备码方式，不读取 PCL 环境变量。真实授权结果以 `packs/launcher-acceptance.json` 为准，模拟测试不替代真人验收。接入依据见 [CmlLib 官方文档](https://cmllib.github.io/CmlLib.Core-wiki/en/auth.microsoft/cmllib.core.auth.microsoft/jeloginhandler/)。
- 14:16 用户本人完成真实微软授权，游戏所有权、DPAPI 会话恢复、静默认证、游戏角色会话和皮肤下载全部通过。第一次测试出现 HTTP 连接异常，未保存任何会话；增加只记录阶段、错误类型和状态码的诊断后，第二次完整通过。未启用不验证所有权的登录或离线降级，未启动游戏。
- 三服模组的自动下载来源已经补齐；原生正式清单仍需接入原版资源、加载器库、配套 Java 和配置策略，再完成游戏验收。没有发布出处的原文件按用户要求自动落到自建源，不再作为发布阻塞理由。
- 群服皮肤的启动器上传/头像链路已实现，游戏内皮肤模组仍待逐服适配和入服验证。
- 地图切线身份、任务书、机器、性能、干净 Windows 无 Java 安装、更新中断/磁盘不足/活跃进程等完整验收未完成。
- 目录迁移、旧端导入、模组管理及诊断已有原生实现，仍需补齐交互验收。

已生成 ECDSA P-256 发布密钥并将公钥嵌入客户端，私钥仅保存在忽略目录 `.local/signing/`。目录最低启动器版本比较已改为读取当前程序集版本，修复 0.1.1 被错误判为 0.1.0 的问题。面向玩家的 Release 工作流检查真实微软验收记录及认证代码指纹，并检查三服自动来源；不再将 Azure 应用 ID 作为 Windows 默认登录的前置条件。

当前仍是开发版本，三服正式内容尚未发布。本轮仅在用户明确同意后打开微软授权窗口，没有拉起启动器大厅或游戏，也没有再打包交付无法使用的 beta。最终门槛不变：三服全自动安装与真实入服通过；三服只使用经过验证的 Cleanroom + Java 25。

本轮补齐：缺原站文件自动托管已实际发布 15 个（约 35 MiB），保留包内声明，不要求额外出处；所有 811 个固定文件均有可用下载地址。当前尚未公开 packwiz 全量目录和三个标准整包，也未切换正式 catalog。前端的场景主题选项目前只改变预览；群服游戏内皮肤、原生完整安装、三服真实入服以及启动器自身自动更新仍需完成。
