# 魔金大帅实施状态

2026-09-05。此记录区分代码完成、隔离测试和正式交付，不能替代三服最终验收。

## 已完成

- 产品名「魔金大帅」，黑色底、扁平控件、单色矢量图标，无 emoji 和装饰说明小字。
- 登录、注册、恢复、大厅、三服详情、线路、设置及下载状态已实现。
- 首次在本机登录成功后先设置游戏文件保存位置，可输入路径或打开文件夹选择器；确认前不能安装或启动。设置后自动记忆，恢复未完成的登录仍返回目录页；旧版已保存的内容目录沿用。取消选择不跳过，非空目录与写入失败不会完成设置。
- 原生宿主移除系统标题栏，开启 WebView2 `IsNonClientRegionSupportEnabled`，使用 `app-region` 和 WPF `WindowChrome`。topbar 左侧不重复品牌名，右侧按钮同步最大化状态。
- 页面滚动限制在内容区；登录表单在 1280×820、1024×656、820×650、900×600 下没有外层或横向溢出。
- 皮肤半身头像替代文字头像，支持标准、纤细、64×32 旧版纹理和外层纹理，本地 Canvas 绘制。微软账号读取官方皮肤，自建账号可上传 PNG；无皮肤时显示默认占位人物。
- 本地 UI 不接收登录令牌；皮肤下载不附带账号凭据。原生层验证纹理地址和 PNG，服务器限制尺寸、解压量、CRC，并清除图片附带元数据。
- C#/.NET 10 + React/TypeScript/WebView2 自包含 Windows 包可构建。UI 资源本地加载，Java 与三服内容不包含在初始包中。
- Identity + PostgreSQL 普通/超级邀请码、固定游戏名、保护名、恢复码、会话轮换、撤销、管理员 SSH 命令已实现。
- 124 的独立 `mc-client-hub` Compose 已部署 API 0.1.1、PostgreSQL、downloads。API 为 `127.0.0.1:18081`，下载同时绑定 `127.0.0.1:18080` 和 `192.168.5.124:18080`。
- 下载公网入口 `http://103.40.14.100:21708` 已接通，13:17 实测健康检查、64 KiB 文件 SHA256、HTTP Range 续传通过。补充回环监听时仅重建 downloads，账号及游戏容器均未重启。
- 23 个原名已保护，另 1 组大小写冲突已整体保留，普通码和超级码均不能认领；未选择角色、合并身份或改动存档。
- 每日数据库备份保留七份，皮肤目录纳入每日备份；API 升级前额外保留数据库快照。此次升级核对其他容器的启动时间未变化。
- 私有仓库 https://github.com/M4rkzzz/mojin-dashuai 已推送初始实现，首次 Windows/API Actions 均通过。后续检查增加无界面 UI 验证。

## 已有测试结果

- 新增设备码页面无界面测试：开始、复制、主动打开网页、授权后目录页、登录码出现前后取消、过期及应用错误；1280/820/640 宽度与 125% 缩放通过。页面不使用 localStorage 或 sessionStorage 保存任何登录信息。

- 62 项 .NET 测试通过：签名、路径、运行时约束、下载、事务恢复、皮肤、首次目录设置，以及设备码公开字段、微软验证地址、Xbox/Minecraft 请求格式、各阶段凭据隔离、取消、所有权和错误处理。
- 124 隔离数据库和 API 容器中 34 项验收通过：邀请码、保护名及大小写冲突、恢复、会话、禁用、限流、秘密不入日志/数据库明文；皮肤鉴权上传、公开无凭据读取、账号隔离及无效更新保留旧皮肤。
- 无界面浏览器通过 12 项登录/注册/恢复布局检查，以及大厅、设置、详情、缩放、减少动画检查。
- 标准、纤细及旧版皮肤使用合成测试纹理核对脸部、衣服、手臂像素和半身显示；未使用真实玩家账号冒充验收。
- 无界面测试覆盖群服登录、微软登录及注册后的目录页、恢复码先保存、选择器取消、写入错误、键盘确认、重新打开后记忆和旧用户直接进入大厅，窗口宽度 1280/820/640、125% 像素缩放下通过检查。账号与选择器使用测试替身，不代替真实微软登录或原生文件夹对话框验收。
- 新 Windows 自包含预览位于 `artifacts/魔金大帅-0.1.1-preview/`。按用户正在玩 CS 的要求，不自动启动窗口；新标题栏的实际拖动、最大化、跨显示器缩放仍待桌面实测。

## 三服内容

- 按本地文件 SHA1 与大小匹配 Modrinth 官方版本 API，为一服 22 个、三服 131 个模组补充完全相同文件的作者直链、项目、版本与许可证来源。只整理来源，没有上传 JAR；实际下载校验和未匹配文件的来源仍待完成。

- 一服来源为用户提供的 `D:/Desktop/魔金大帅`，MSE/魔法金属 8.0.4-2，114 个活动 JAR；发布前须排除个人数据和凭据。
- 二服 DeceasedCraft 5.10.17 中文 r3 的 317 个引用仍需核对自动来源及 ModernFix 兼容。
- 三服从桌面旧客户端整理独立 `.local/lab/mb`，使用官方 Cleanroom 0.5.17-alpha 与 Temurin 25.0.4.1+1，补齐安装器生成的库路径和原版 JAR 引用。已停用的机器注册项未重新引入。
- 三服隔离进程日志在 12:12:44 报告成功加载 422 个模组，12:17:56 正常退出，退出码 0。这只证明本地加载流程，不能当作入服、地图、任务书、机器或性能验收。
- Java 8、17 的官方分发包已下载并核对哈希；Java 25 使用已准备的固定发行版。三服严格禁止 Java 8/17 回退。

## 外部配置及剩余工作

- Cloudflare 凭据问题已解决。用户最新要求在 `D:/project/cfapi/credentials.json` 明文保存可复用 Global API Key；已保存并验证，不再依赖原 GitHub 项目。密钥文件位于 Git 仓库之外，未上传 GitHub 或 124。使用方式见 `docs/CLOUDFLARE-CREDENTIALS.md`。
- 已创建专用 Tunnel `2aef8300-5e56-4c26-9abd-0d3019c5fffd` 与 `launcher.boshan.uk` DNS。124 的 cloudflared 使用 HTTP/2，已建立两条连接；只有该容器启动/重建，其他容器启动时间未变化。配置规则只对该 API 主机关闭 Browser Integrity Check，保留其他域名规则。
- `https://launcher.boshan.uk/health` 与公告接口已返回 200。18 项公网及 Windows 原生检查通过：注册、固定游戏名、登录、刷新、退出、旧令牌撤销、原生 DPAPI 保存、恢复登录、游戏会话昵称；测试账号和邀请码已删除。目录接口仍按预期返回 503，因为正式游戏内容尚未验收发布。
- 正版登录改为与 PCL 相同的微软设备码流程，保留 MSAL 缓存与静默刷新。页面支持获取中、复制、主动打开微软官网、倒计时、取消、拒绝和过期；浏览器只接收用户登录码、地址与到期时间。已修复 Xbox 请求字段被 JSON 默认策略改成小写的问题，并区分应用被拒绝与账号未拥有游戏。公开 PCL 源码从 `PCL_MS_CLIENT_ID` 读取应用 ID，没有提供公共登录代理或内置可用 ID。本项目 `microsoftClientId` 仍为空，真实正版授权与皮肤尚未验收；没有将页面/协议模拟测试当作真人登录通过。
- 三服内容及运行时仍需逐文件完成自动来源、分发依据与正式签名清单；不能将本地已有文件视为有权公开分发。
- 群服皮肤的启动器上传/头像链路已实现，游戏内皮肤模组仍待逐服适配和入服验证。
- 地图切线身份、任务书、机器、性能、干净 Windows 无 Java 安装、更新中断/磁盘不足/活跃进程等完整验收未完成。
- 目录迁移、旧端导入、模组管理及诊断已有原生实现，仍需补齐交互验收。

已生成 ECDSA P-256 发布密钥并将公钥嵌入客户端，私钥仅保存在忽略目录 `.local/signing/`。目录最低启动器版本比较已改为读取当前程序集版本，修复 0.1.1 被错误判为 0.1.0 的问题。面向玩家的 Release 工作流新增配置与来源检查，当前仍会因微软应用 ID 和三服来源未完成而阻止发布。

当前仍是开发版本，三服正式内容尚未发布。本轮没有拉起启动器或游戏窗口，也没有再打包交付无法使用的 beta。最终门槛不变：三服全自动安装与真实入服通过；三服只使用经过验证的 Cleanroom + Java 25。
