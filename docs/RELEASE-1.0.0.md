# 1.0.0 发布准备

用户已授权 2026-09-06 北京时间 15:30 发布 1.0.0，并逐服短暂重启上线“仅统一端入服”认证。自动化 ID 为 `15-1-0`，本任务一次性执行。

当前状态：正在实现和集成；不得将本文件当作已发布或认证验收通过。机器可读状态见 `packs/release-1.0.0-preparation.json`，必须以最终真实结果为准。

## 范围

- 四服主题加载窗、真实总进度、99%/100% 保持、游戏窗口交接和 GLFW 辅助窗口排除。
- 群服及微软账号的短期入服授权、一次性票据、每进程本机 IPC、服务端登录前核验。
- 四服 r4 完整包已由四服任务最终交付，接入一次渲染配置迁移，保留玩家地图、设置和内存；先完成更新连接检查再激活。清单最低启动器版本须为 1.0.0。
- 版本号 1.0.0，启动器和游戏内容仍分别更新；未通过的干净 Windows 和部分皮肤目视验收不回填为通过。

## 上线顺序

1. 完成签名、安装包和逐文件更新包；提前只上传下载对象，暂不激活更新指针与目录。
2. 到达 15:30 后，备份并部署 Hub API，初始化表，核对现有身份映射；没有证据的同名跨账号不自动合并。
3. 逐服备份游戏启动脚本和认证组件，正常停止、启动，以 observe 模式加载最终认证代理。确认四服健康并能接收新版握手后，才激活 1.0 启动器更新和内容目录，避免新版票据握手进入旧服务器。
4. 完成三套版本协议的真实正常认证与未授权连接拒绝检查，验证票据不能重放，再逐服切换 enforce。未完成的目标不启用拦截、不为尚未可用的功能空重启；只操作必要服务，不重启整台主机或 gsmanager。
5. 更新发布记录并报告实际成功目标；已执行过的目标不重复重启。任何旧候选被兼容修复替代后，必须重新固化最终代理和安装包哈希。

## 待补记录

认证组件哈希、API 镜像、数据库迁移结果、微软身份关联结果、逐服运行命令与回滚路径、签名清单及安装包哈希、四服 r4 更新连接记录均须在完成后写入准备 JSON。当前尚未完成。

