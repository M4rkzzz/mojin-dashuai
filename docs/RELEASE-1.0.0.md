# 1.0.0 发布准备

用户已授权 2026-09-06 北京时间 15:30 发布 1.0.0，并逐服短暂重启上线“仅统一端入服”认证。自动化 ID 为 `15-1-0`，本任务一次性执行。

API 1.1.0 已于 15:30:39 激活，最终 1.0 客户端更新指针 sequence 19、内容目录 sequence 8（最低启动器 1.0.0）已由发布负责人激活并公开读回通过。四服现在全部健康运行于 **enforce** 模式。四服真实账号入服、生产 API 一次性票据核销、测试客户端正常退出，以及实际无票据连接的明确统一端拒绝均已通过。认证热切换未重启任何游戏，四服 PID/启动 ticks 均保持。

二服原认证钩子的启动兼容问题和三服 Forge 解码提前移除票据的问题，已由服务端专用修正版解决。二服维护已解除，二、三服真实验证均通过；一、四服保留已经验收的原代理，未为这次修复额外重启。客户端代理仍为 d870 版本。

运行与验收记录见 [server-deployment.json](../artifacts/join-auth-1.0.0/server-deployment.json)。发布指针及安装包的准备状态由 `packs/release-1.0.0-preparation.json` 单独记录。

| 服务器 | 当前 PID / 启动 ticks | 服务端代理 | 实际入服时间（北京时间） | 票据签发 / 核销 | 无票据拒绝通过（北京时间） | 当前模式 |
| --- | --- | --- | --- | --- | --- | --- |
| 一服 m3e | 296471 / 137149000 | d870e725…ce43d6 | 15:35:49 | 1 / 1 | 16:24:34 | healthy enforce |
| 二服 dc2 | 331850 / 137324515 | acd591d7…a01f66 | 16:03:14 | 1 / 1 | 16:27:22 | healthy enforce |
| 三服 mb | 337293 / 137359101 | acd591d7…a01f66 | 16:11:51 | 1 / 1 | 16:27:24 | healthy enforce |
| 四服 vw | 307926 / 137197530 | d870e725…ce43d6 | 15:48:18 | 1 / 1 | 16:24:41 | healthy enforce |

客户端及一、四服代理 SHA256：`d870e72522897d529ef35ac5b1f48fb8478216eff5eb4e44de2b9495fdce43d6`。

二、三服服务端专用代理 SHA256：`acd591d737aff3e34ae4e87221729a807cb660926235e038ecf2d8e6e9a01f66`。

## 范围

- 四服主题加载窗、真实总进度、99%/100% 保持、游戏窗口交接和 GLFW 辅助窗口排除。
- 群服及微软账号的短期入服授权、一次性票据、每进程本机 IPC、服务端登录前核验。
- 四服 r4 完整包已由四服任务最终交付，接入一次渲染配置迁移，保留玩家地图、设置和内存；先完成更新连接检查再激活。清单最低启动器版本须为 1.0.0。
- 二服汉化资源包的一次性启用迁移已接入启动准备：仅在汉化 ZIP 存在时启用，保留原 options 备份、其他资源包顺序和玩家明确的语言选择；后续主动关闭不再强开。9 项针对性测试通过；发布负责人已确认最终 ResourceManager 实际加载汉化包、正常入服及退出。
- 版本号 1.0.0，启动器和游戏内容仍分别更新；未通过的干净 Windows 和部分皮肤目视验收不回填为通过。

## 上线结果

1. 发布负责人完成最终语言验证并激活 1.0 启动器更新指针与内容目录，公开读回通过。
2. 四服逐一通过 `activate-game-join.py --mode <id> enforce` 热切换，仅原子修改认证模式，没有重启游戏。
3. 四服实际无票据连接均收到“请使用魔金大帅统一客户端启动本服务器”的明确拒绝。二、三服探针分别携带真实字节码确认的 `\0FML3\0`、`\0FML\0` Forge 标记，但没有 MOJIN 票据，确保测试的是认证拦截。
4. 二、三服第一轮原版握手先被 Forge 拒绝，当时立即恢复 observe；这两条结果作为非认证验收历史保留。修正探针后两服重新切 enforce 并实际通过，不把 Forge 的通用拒绝冒充统一端认证通过。

## 证据与边界

四服的实际服务器入服日志和生产 API 核销查询已汇总到部署 JSON，每服恰好签发、消费一张票据，测试客户端退出码均为 0。API 查询原始脱敏记录位于 `artifacts/api-1.1.0/consumption-<id>-20260906.json`；这些记录不包含票据正文、票据哈希或服务端私钥。

首次启动脚本备份始终保留；二、三服专用代理替换还保存了上一代候选。完整部署命令与回滚位置见 [GAME-JOIN-ROLLOUT.md](../deploy/GAME-JOIN-ROLLOUT.md)。

最终热切换时间、PID、明确拒绝内容及两条原版握手历史见 [missing-ticket-acceptance.json](../artifacts/join-auth-1.0.0/missing-ticket-acceptance.json)。更新指针与安装包哈希继续由发布负责人在准备记录中统一维护，最终提交和 GitHub 发布也由其完成。

干净 Windows、未完成的皮肤目视验收继续保留未验收状态，不因本次认证通过而改写为通过。

