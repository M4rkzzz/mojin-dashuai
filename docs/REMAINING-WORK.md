# 尚未完成的部分

2026-09-05。缺少原站的 15 个文件已自动补传，三个服共 811 个固定文件都有下载地址。文件发布只需必要的哈希、大小和可用性检查，不再等待发布出处。

| 顺序 | 当前缺口 | 下一步 |
|---|---|---|
| 1 | 一、二服原版本的 M4rkzzz 入服及操作检查通过；三服修正版已在初始化结束后自动连接，加载 424 个模组 | 等用户确认“登入中”遮罩消失，以及地图、任务书、机器界面、皮肤与性能 |
| 2 | 三个标准整包、packwiz 目录及原生运行时/依赖已上传，正式 catalog 仍返回 503 | 游戏验收通过后再签名并切换正式目录；后续修正的内容对象按新哈希补传 |
| 3 | 从空目录完整网络下载的检查正在进行；干净 Windows 尚未验收 | `tools/check-network-install.py` 使用空缓存、原生安装器及每秒 2 MiB 限速；完成后记录结果，仍不能冒充干净 Windows 验收 |
| 4 | 皮肤公开元数据/纹理 API 已部署，三服标准包的皮肤配置及必要模组已发布 | 逐服确认游戏内皮肤显示；一、二服新增配置后的实际游戏复测尚未完成 |
| 5 | 场景主题已接通实际页面，默认黑色；启动器自身自动更新尚未实现 | 补启动器版本检查及升级流程 |

迁移、旧端导入、模组管理已有原生代码，最终安装流程贯通时检查一次真实操作即可。既有微软授权、账号服务、下载续传和签名测试不因文件补传重复执行；只有相关代码变化或出现问题才复测。

一服 Angelica 的重复 `SKIPPING glBindTexture for target 32879` 已在客户端连接组件中加入过滤，保留第一次 INFO，抑制后续同一条 INFO；警告、错误及其他内容全部保留。用实际 Log4j 2.0-beta9 管线检查通过，下一次一服游戏测试再检查日志增量。该调用仍由 Angelica 交给 OpenGL 执行，没有修改渲染逻辑或更换渲染模组。

二服补齐 Forge 安装器定义的八个运行文件。Windows 下的 `failed to open a new selector` 已通过实例内的短相对 socket 临时目录修复；随包 Java 17、25 的 `Selector.open()` 实测通过，二服随后自动连接成功并正常退出。没有修改生产服务器或更换 Java 版本。

本轮已修复已安装实例启动前不检查更新、暂停后续传不继续启动，以及修改密码后页面仍保持登录三个问题。自动更新保留显式授权的回退版本；临时服务故障可使用已安装内容，不把无效签名当成网络故障。

以上缺口补齐后再切换正式签名目录和交付可用 beta。整个过程不修改或重启现有游戏服。
