# 尚未完成的部分

2026-09-05。三个服均已用保存的正版账号 M4rkzzz 入服，用户确认一、二服皮肤和运行正常，以及三服地图、任务书、机器界面正常。三服首次连接的“登入中”遮罩已消失。一服重复渲染日志实测只保留一次。全部游戏正常退出，没有修改或重启生产游戏服。三服的空缓存网络下载和 Java 8/17/25 安装均已完成，记录见 `packs/network-install-acceptance.json`。

| 当前缺口 | 下一步 |
|---|---|
| 干净 Windows 验收未完成 | 用户暂时没有另一台电脑或虚拟机，当前电脑沙盒未启用；暂挂此项，不重启当前电脑、不将现有环境冒充干净系统 |
| 两条线路地图实测已通过 | 一、二服写入实例内共享 Xaero 目录；三服第二条线路继续写入已有 JourneyMap 目录，原记录均保留 |
| 群服账号自建皮肤尚未在游戏内独立观察 | API 模型、纹理、鉴权和隔离测试通过；一、二服正版皮肤已确认，三服官方皮肤请求通过，自建 API 无该角色皮肤时正确返回 404 |
| 启动器设置按钮人工操作待最终检查 | 实际 beta.3 → beta.4 WPF 进程接管已通过：旧程序退出、新版窗口就绪、新程序正常关闭；设置按钮已有前端测试，未人工点击 |
| 正式签名目录未激活 | 补齐验收后签名并切换目录，完成端到端安装检查，再交付 beta |

启动器自身更新已实现，API 0.1.4 已部署，候选 0.1.2-beta.4 ZIP 已上传，公网完整下载与 492 个文件校验通过；本机已准备该候选。`/v1/catalog` 仍返回 503，`/v1/launcher` 仍返回 404，没有向玩家推送未完成验收的内容。方法见 [启动器更新](LAUNCHER-UPDATES.md)。

旧端导入与迁移的实际文件操作已通过隔离检查：保留旧目录和当前设置、冲突文件另存、排除账号缓存和 FRP、不递归导入自身。迁移期间阻止其他内容操作，并获取三个实例锁。文件选择器交互仍需在最终安装流程中检查。

没有发布出处的原文件按用户要求落到自建源，不再阻塞；仅保留必要的大小、哈希和可用性检查。肉丸工艺只使用 Cleanroom + Java 25，无 Java 8/17 回退。
